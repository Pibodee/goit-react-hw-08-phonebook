import styled from '@emotion/styled';

export const NavBar = styled.header`
  position: sticky;
  top: 0;
  left: 0;
  display: flex;
  gap: 20px;
  align-items: center;
  background-color: #1c1c1e;
  height: 60px;
  color: #feb340;
`;
